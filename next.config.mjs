/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', 'example.com'], // Thêm tên miền vào đây
    },
    reactStrictMode: true
};

export default nextConfig;
